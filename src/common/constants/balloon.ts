/**
 * 풍선 터뜨리기(러시안 룰렛식) 정책.
 * 가운데 풍선 하나를 참가자들이 순서대로 한 번씩 펌프(공기 주입)한다.
 * 호스트가 풍선 크기(=최대 펌프 수)를 정해 시작하면, 서버가 1..크기 사이의 비밀 '터지는 순번'을
 * 정한다. 누적 펌프 수가 그 순번에 도달하는 순간 풍선이 펑 터지고, 그때 펌프한 사람이 '걸림'(패배)이
 * 되며 게임이 끝난다.
 */
export const BALLOON = {
  /** 풍선 최소 크기(최대 펌프 수) */
  MIN_TOTAL: 3,
  /** 풍선 최대 크기(최대 펌프 수) */
  MAX_TOTAL: 30,
  /** 기본 풍선 크기 */
  DEFAULT_TOTAL: 12,
  /** 게임이 성립하려면 필요한 최소 참가자 수(턴을 돌리려면 2명 이상) */
  MIN_PLAYERS: 2,
} as const;
