/**
 * 방 정원 정책 (한곳에서 관리).
 * 호스트가 방마다 정원을 정하고, 안 정하면 DEFAULT. 하드 상한은 MAX 를 넘을 수 없다.
 */
export const ROOM_CAPACITY = {
  /** 최소 정원(게임이 성립하려면 최소 인원) */
  MIN: 2,
  /** 하드 상한 — 호스트가 이보다 크게 설정할 수 없다 */
  MAX: 200,
  /** 호스트가 따로 설정하지 않았을 때 기본 정원 */
  DEFAULT: 200,
} as const;
