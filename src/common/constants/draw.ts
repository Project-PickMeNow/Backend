/**
 * 제비뽑기(인터랙티브) 정책.
 * 호스트가 인원수(제비 개수 N)와 꽝 개수 K 를 정하고 "제비 섞기"를 누르면
 * 서버가 K개의 꽝 위치를 무작위로 배치한다. 방장·참가자가 각자 제비를 뽑고,
 * 먼저 뽑힌 제비는 잠긴다(중복 불가).
 */
export const DRAW = {
  /** 최소 제비 수 */
  MIN: 2,
  /** 최대 제비 수 */
  MAX: 12,
  /** 참가자 1인당 뽑기 상한(항상 1개). 호스트만 이 상한과 무관하게 여러 개를 뽑을 수 있다. */
  PER_PICK: 1,
  /** 뽑은 사람 표기 — 호스트의 첫 제비. */
  HOST_NAME: '호스트',
  /**
   * 뽑은 사람 표기 — 아무도 고르지 않은 제비.
   * 호스트가 2번째부터 뽑은 제비, 그리고 60초가 지나 자동 공개된 제비는 이 이름으로 표시한다.
   */
  UNSELECTED: '미선택',
  /** 라운드 시작 후 이 시간(초)이 지나면 아직 안 뽑힌 제비를 자동으로 '미선택'으로 공개한다. */
  AUTO_RESOLVE_SECONDS: 60,
} as const;
